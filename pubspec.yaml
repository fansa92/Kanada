name: kanada
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  kanada_album_art:
    path: ./kanada_album_art
  cupertino_icons: ^1.0.8
  dynamic_color: ^1.7.0
  audio_service: ^0.18.18
  just_audio: ^0.9.46
  just_audio_background: ^0.0.1-beta.16
  permission_handler: ^12.0.0+1
  fluttertoast: ^8.2.12
  audio_metadata_reader: ^1.3.0
  flutter_launcher_icons: ^0.14.3

publish_to: none

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true   # 为 Android 生成图标
  ios: false       # 为 iOS 生成图标
  image_path: "assets/icon/Kanade.png"  # 图标源文件路径
  # 可选参数（按需配置）：
  adaptive_icon_background: "#FFFFFF"  # Android 自适应图标背景色
  adaptive_icon_foreground: "assets/icon/Kanade.png"  # 前景图层